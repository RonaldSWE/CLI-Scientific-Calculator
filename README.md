# 🧮 Scientific Calculator in C++

A simple **console-based scientific calculator** built with **C++**, focused on **trigonometric and scientific functions**.  
This project is intended for **learning and practice**, especially working with the C++ math library.

---

## ✨ Features

This calculator supports:

- Sine (`sin`)
- Cosine (`cos`)
- Tangent (`tan`)
- Logarithm (`log`)

> **Note:**  
> Basic arithmetic operations (`+ - * /`) are intentionally not included.  
> The goal of this project is to focus purely on **scientific calculations**.

---

## 🛠️ Built With

- **Language:** C++
- **Library:** `<cmath>`
- **Type:** Console application

---

## 🚀 How to Run

1. Make sure a C++ compiler is installed (e.g. `g++`)
2. Compile the program:
   ```bash
   g++ Cal.cpp -o Cal
   ```
